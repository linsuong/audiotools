import numpy as np
import matplotlib.pyplot as plt

class Room(self, size, reflectorType):
    """
    generates a 4 sided structure given specific dimensions

    Args:
        size (list): breadth, width, height of a room.
        reflectorType (str): specifies the type of reflector.
            perfect - a perfect reflector, i.e. a flat wall.
            treated - a treated room with acoustic panels.
            rough - a wall that is slightly rough.
    """

    b, l, w = size

    
class MicrophoneSource(self, x, y, z):

def imageSource():
    return

def rayTracing():
    return None

def FDTD():
    return None
